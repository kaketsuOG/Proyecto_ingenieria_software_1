import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

const routes: Routes = [
  { path: '', component: LoginComponent }, // Configura el componente LoginComponent como la página principal
  // Otras rutas aquí...
  
];


